<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.ac.uc.webchatting.dao.IChatRoomUserInfoDAO">
    <insert id="insertChatRoomUserInfo" parameterType="kr.ac.uc.webchatting.dto.ChatRoomUserInfoDTO">
        insert into chat_room_userinfo(room_id, id, authority)
                    values (#{room_id}, #{id}, #{authority});
    </insert>
    <select id="checkUserInChatRoom" parameterType="string" resultType="_int">
        select COUNT(*) from chat_room_userinfo where room_id=#{room_id} and id=#{id};
    </select>
</mapper>